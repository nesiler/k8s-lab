apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: test-lab
  labels:
    app: grafana
    component: monitoring
spec:
  type: LoadBalancer
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: grafana